/** tokens ONLY — defineTheme arrives via the auto-using prelude (zero imports needed). */
export default defineTheme({ name: 'fsdemo', color: { primary: '#0A2230', accent: '#7C6CFB' }, font: { head: 'Poppins' }, mode: 'literal' });
